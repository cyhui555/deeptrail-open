# 需求注册与追踪矩阵

- 文档状态：旅迹 v0.1.0 已封板；后台运营第一期已交付
- 最近更新：2026-07-17
- 产品：旅迹

| Requirement ID | 用户或工程能力 | 状态 | 验收结论 |
| --- | --- | --- | --- |
| REQ-MIGRATION-001 | 将 `D:\ai\travel` 当前有效工作树迁入新工程 | 已完成 | 业务源码、测试、PWA 资源和现有安全修复完整保留；未携带源 `.git`、缓存、日志、数据库和调试产物 |
| REQ-ARCH-001 | 按参考工程建立稳定 monorepo 边界 | 已完成 | `apps/web`、`apps/server`、`database`、`docs`、`evals`、`tests/e2e`、`infra` 职责清晰，根命令统一编排 |
| REQ-RUNTIME-001 | 在无真实第三方密钥时可启动核心程序 | 已完成 | BUG-20260715-004 已恢复 8080 后端；JAR Health 与 Web 登录页返回 200，真实浏览器无 5xx，测试使用本地确定性替身 |
| REQ-QUALITY-001 | 迁移后质量门禁不低于源工程 | 已完成 | 封板门禁后端 635/635、服务端 E2E 37/37、Contract 通过、Playwright 全量 119 条为 118 通过/1 条件跳过/0 失败，以及 lint、typecheck、生产构建和 11/11 路由预算全部通过 |
| REQ-SECURITY-001 | 密钥、用户数据和本地产物不进入新仓库 | 已完成 | 配置引用环境变量；疑似密钥扫描无匹配；忽略规则覆盖数据库、上传、日志和测试产物 |
| REQ-SECURITY-002 | 公网部署前消除认证和媒体访问高风险 | 已完成 | 媒体访问校验所有权、路径与真实文件头；JWT 使用 HttpOnly Cookie；生产关闭敏感调试入口和日志 |
| REQ-DATA-001 | 数据库结构使用可审计、失败即停止的版本迁移 | 已完成 | Flyway 是唯一迁移入口；旧库可基线升级；新库可从空库创建；迁移错误阻止启动 |
| REQ-DEPLOY-001 | 容器和生产配置在不同环境中保持可预测 | 已完成 | 同源 API 路由、standalone 构建、健康检查、运行时环境变量和生产 profile 已验证 |
| REQ-MAINTAINABILITY-001 | 降低大类和手工 API 契约的变更风险 | 已完成 | 第一阶段服务职责拆分；OpenAPI 契约基线已在自动测试检查漂移 |
| REQ-BRAND-001 | 将产品统一为“旅轨”并建立一致的界面视觉语言 | 已完成 | 品牌、元数据、PWA、认证页、应用外壳和核心页面已统一；保留毛玻璃并完成桌面与移动端验收 |
| REQ-BRAND-002 | 按确认参考图统一全站暖色编辑式视觉 | 已完成 | 暖纸张、赤陶强调、编辑式标题、侧边目录、移动端布局、日程与地图视觉已统一；保留路由和业务契约 |
| REQ-BRAND-003 | 保留纸张底图并将 UI 恢复为蓝色，完成手机端适配 | 已完成 | 品牌、按钮、导航、焦点与计划路线统一为矿物蓝；390px、360px 核心页面零横向溢出，输入聚焦时底栏主动避让 |
| REQ-BRAND-004 | 为更多页面增补新疆、川西气质的实景风景背景 | 已完成 | 认证、规划、行程、资料和详情页共享雪山、大山、河谷草原与高原公路真实旅行摄影；横竖构图、内容可读性与 390px/360px 稳定性已验收 |
| REQ-BRAND-005 | PDF 导出与页面视觉统一，并保证合理字号和稳定分页 | 已完成 | 暖纸张与矿物蓝视觉已统一；固定 A4 画布、卡片级分页、长文本续页和路线索引通过 8 页真实 PDF 视觉验收 |
| REQ-UX-001 | 统一主要按钮语义、恢复地图可诊断性并将行程首屏压缩到 1 秒目标内 | 已完成 | BUG-20260715-003 第二轮已移除签到 GET 同步地理编码和媒体 N+1；晚到 GPS/坐标可校正视野；生产受控 300ms + 300ms 为 494ms |
| REQ-AI-001 | 使用可替换且可验证的真实模型 Provider | 已完成 | LongCat 运行时配置已安全注入本机与目标机 root-only 环境；`LongCat-2.0` 直连、Spring AI 结构化行程和生产容器最小 Provider 探针均复验通过，测试桩仅允许 test Profile |
| REQ-UX-002 | 在 AI 未就绪时提供清晰状态，并保持首页规划操作连续、刷新稳定和错误可恢复 | 已完成 | 安全 AI 状态、影响范围与重试入口已上线；跨 Tab 草稿保留、静默轮询、错误恢复和立即提交通过真实桌面/移动端验收 |
| REQ-PERF-001 | 降低行程概览页首载体积，避免未使用 PDF 能力阻塞首屏 | 已完成 | PDF 与静态地图模块已改为点击后加载；Route `169→5.79 kB`、First Load JS `275→112 kB`，预算与 PDF 下载回归通过 |
| REQ-BRAND-006 | 将当前产品名称从“旅轨”统一更改为“旅迹” | 已完成 | 用户可见表面、元数据、PWA、当前文档和工程记忆统一更名；历史归档保持原始事实 |
| REQ-PRODUCT-001 | 从“生成行程”升级为“执行一次旅行” | 已完成 | 首页优先承接进行中或即将出发的行程，规划、现场执行和旅行回忆形成连续入口 |
| REQ-UX-003 | 重构首页任务优先级并收敛行程信息架构、视觉反馈、编辑和地图联动 | 已完成 | 三阶段行程工作区、统一反馈、跨会话草稿和地图列表双向选择已通过移动端、键盘与浏览器回归 |
| REQ-PERF-002 | 建立全路由预算、真实用户指标、SSE、分页、地图媒体和 PWA 性能边界 | 已完成 | 11 条关键路由预算、脱敏指标、SSE 回退、增量列表、轨迹媒体与离线隔离通过自动化门禁 |
| REQ-DOCS-001 | 精简沉淀文档、建立工具约束并接入 deepbarin 工程记忆 | 已完成 | 本地文档已分层压缩并加入自动预算检查；deepbarin 校验 6/0 通过，因源同步范围 dirty 按协议保持 `partial/paused` |
| REQ-QUALITY-002 | 并行审计并消除 M12 交付后的 UI、性能与回归剩余缺口 | 已完成 | 统一弹窗与现场可访问性、轨迹归属校验、任务竞态、离线队列及 4 条浏览器边界回归通过全量门禁 |
| REQ-DATA-002 | 离线打卡与轨迹在断线、超时和成功响应丢失后可安全重放 | 已完成 | IndexedDB v2 在首次发网前为 v1 旧队列补齐并持久化客户端标识，连接重置后重试复用原标识且队列不重不丢；所有权优先校验、打卡原子状态与计数及 Flyway V4 任务/行程范围唯一约束保证安全重放，旧客户端与跨范围隔离通过回归；V5 将旧无时区轨迹按 UTC+08:00 迁移到 UTC，API 始终回传 `Z`，混合合法/非法批次失败时零新增 |
| REQ-QUALITY-003 | 浏览器回归以确定性状态替代固定等待并降低偶发失败 | 已完成 | `waitForTimeout` 由 15 处清零，约 93 处位置选择器仅保留 3 个业务顺序用法，`force/networkidle` 为 0；确定性地图就绪信号与连续两轮全量 116 条均为 115 通过/1 条件跳过/0 失败完成验收 |
| REQ-RELEASE-001 | 固化旅迹首个稳定发布基线 | 已完成 | 用户确认封板；完整门禁通过，创建本地 `v0.1.0` Git revision 并同步工程恢复记忆；未推送远程发布 |
| REQ-OPS-001 | 建立后台运营入口并由管理员统一分配用户账号 | 已完成 | 默认环境关闭公开注册；管理员角色、账号初始化、用户查询/创建/启停/重置密码、越权拦截及响应式管理界面通过服务端与浏览器验收 |
| REQ-DEPLOY-002 | 将当前旅迹版本可追溯地部署到目标 Linux 主机并沉淀后续发版脚本 | 已完成 | `v0.2.0` 已以不可变 release 发布到受控目标主机的 `30301` 端口；备份、迁移、健康、重启、外网、管理员认证和浏览器验收通过，见[目标环境发布报告](../archive/task-release-002-production-deployment.md)；远程 CI、TLS、独立 Restore 等仍阻断完整生产放行 |
| REQ-LOOP-001 | 在不重写业务程序的前提下，将工程接入固定版本 LoopAny 并形成可审计的本地影子闭环 | 已完成 | PR #19 已按 deepbarin 规范补齐 ExecutionSpec/Outcome、分阶段恢复、隔离 Backup/Restore 与可执行手册；L0 和 L1 Phase 1 G0—G3 通过，业务反馈、治理进化和 L2/L3 保持 `NOT READY` |
| REQ-LOOP-002 | 将 Loop 工程推进到持续可验证的 L1 Phase 2，并以脱敏 Release Outcome 和稳定 Shadow 证据完成 L2 Proposal-only 准入 | 进行中 | PR #22/#23 已实证单维护者受保护合并与第 4 项 Shadow；正式 L2 仍由两个 Bug 的目标环境复验及公开 Cohort 从 4/10 补齐到 10/10 阻断 |

## 范围说明

本次迁移保持现有产品行为和 API 契约，不进行 Spring Boot、Spring AI、Next.js 或 React 的跨主版本升级，不新增业务能力，也不调用需要真实密钥或产生费用的网络测试。当前环境未安装 Docker，容器构建不计入本机验收结论。
