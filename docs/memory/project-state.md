# 当前项目状态
- 最后核对：2026-07-19
- 当前阶段：`BUG-20260719-001` 实现与本地 G2 门禁完成，准备提交 PR；L3B Engine 保持休眠
- 当前检查门：公开 `main@20afc03`；短期分支必须经 Required Checks 与审批合同合入，用户已单独批准本 Bug 合入后的不可变部署
- 活动工作项：`BUG-20260719-001`、`TASK-LOOP-008`、`TASK-RELEASE-003`、`TASK-OPS-002`

## 当前事实
- PR #50 五项生产 E2E 根因已合入并发布为 `v0.2.0-20260718-163614-20afc03e084e`；最终生产 E2E 30/30、容器健康、日志与 SQLite 收尾检查通过。
- 新生产反馈的 3/9 根因已修复：强制重查失败保留旧值，9 点受控并发，高德地址空结果转 POI 搜索，Web 使用独立超时并展示真实完成度。
- 未认证浏览器被引导至登录页，未认证行程 API 返回 401；本次取证未绕过用户归属或读取真实 POI 内容。
- 旅迹 M0—M16、后台运营与 v0.2.0 不可变发布历史位于 `docs/archive/`；L3A 已交付，L3B activation 已终止且无合并权限。

## 当前约束
- 禁止直推 `main`；必须经短期分支 PR、有效审批和 Required Checks。仅 `BUG-20260719-001` 在合入后按用户本次明确批准执行部署。
- 默认测试使用确定性替身；真实高德探针只在人工触发的 release 验收执行一次并声明范围，不读取用户数据。
- 完整生产放行仍缺 TLS、凭据轮换、远程制品链、独立介质 Restore 和正式回滚演练。

## 当前验证
- 基线验证：PR #50 远程 CI 与 `main` CI 全绿；生产 E2E 30/30，当前 release 健康。
- 本 Bug G0：真实入口 200，Playwright 确认认证重定向且无页面错误，两个行程 API 未认证均为 401；代码与官方接口契约审计完成。
- 本 Bug G2：Server 677/677、后端 E2E 39/39、Playwright 6/6、smoke 12/12；lint、typecheck、OpenAPI、覆盖率、构建、体积、Eval、文档与工作项均通过。

## 下一项唯一动作
提交并推送短期分支，创建 PR，等待 Required Checks 与独立审批合同满足后合并。
