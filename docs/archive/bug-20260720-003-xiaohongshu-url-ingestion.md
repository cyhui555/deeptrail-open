# BUG-20260720-003 小红书链接导入修复摘要

- 状态：G3 / Closed
- Requirement：`REQ-AI-001`、`REQ-UX-002`
- 根因：首页正文模式把裸短链作为正文提交；小红书页面状态含字符串外的 JavaScript `undefined`，严格 JSON 解析失败后误取通用描述。
- 交付：前后端在各自信任边界识别完整小红书 URL，抓取器受域名白名单保护并优先读取目标笔记结构；失败时继续关闭，不让模型猜测。
- 验收：Server 684/684、本地 smoke 16/16、真实短链只读抓取 1/1、安全合同 22/22；PR #75 最终 Head `c3e268c` 的五项 Required Checks 成功，并合入产品基线 `1dcaa19`。
- 边界：未调用真实 AI，未部署，未修改或重算历史任务；后续目标环境验证必须从执行时解析的受保护 `main` 构建。
- 回滚：回退 PR #75 的表单归一化、内容服务 URL 识别、状态解析器及测试；不涉及数据库、API Schema 或生产配置。
